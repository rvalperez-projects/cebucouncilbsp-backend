<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property resource="application.properties"/>

  <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-50c{5} - %m%n</pattern>
    </encoder>
  </appender>

  <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${log.file.path}%d{yyyy-MM-dd}${log.file.extension}</fileNamePattern>
      <maxHistory>${log.file.max.history}</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5p %-32X{traceKey:-________________________________} %c{5}.%M - %m - [th:%thread, cc:%X{companyCode:-__}, umn:%X{userMngNo:-__}, di:%X{deviceId:-__}, ac:%X{applicationCode:-__}, uri:%X{requestURI:-__}]%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <!-- Application Loggers -->
  <logger name="com.cebucouncilbsp.backend" level="${log.level.project}"/>

  <!-- 3rdparty Loggers -->
  <logger name="org.springframework.web" level="${log.level.spring.web}"/>

  <logger name="org.mybatis" level="${log.level.mybatis}"/>

  <root level="info">
    <appender-ref ref="console"/>
    <appender-ref ref="file"/>
  </root>
</configuration>
